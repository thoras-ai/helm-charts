---
apiVersion: v1
kind: ServiceAccount
metadata:
  labels:
    {{- include "thoras.resourceLabels" (dict "root" . "component" .Values.thorasForecast.worker.labels) | nindent 4 }}
  name: {{ .Values.thorasForecast.serviceAccount.name }}
  namespace: {{ .Release.Namespace }}
imagePullSecrets:
  - name: {{ include "thoras.registrySecretName" . }}
