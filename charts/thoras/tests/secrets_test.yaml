suite: Secrets
tests:
  - it: Creates the registry secret if password is set
    template: registry-secret.yaml
    set:
      imageCredentials:
        password: "frou-frou"
    asserts:
      - isKind:
          of: Secret
  - it: Doesn't create the registry secret if secretRef set
    template: registry-secret.yaml
    set:
      imageCredentials:
        secretRef: "frou-frou"
    asserts:
      - hasDocuments:
          count: 0
  # Note: The validation that fails when both password and secretRef are defined
  # works correctly in practice (verified with `helm template`) but helm-unittest
  # doesn't properly evaluate {{ fail }} directives when testing templates in isolation.
  # The validation exists in both registry-secret.yaml and operator/webhooks-certgen-registry-secret.yaml
  - it: Uses the reference name if it's passed
    templates:
      - collector/service-account.yaml
      - operator/service-account.yaml
      - dashboard/service-account.yaml
      - api-server-v2/service-account.yaml
      - forecast-worker/service-account.yaml
      - worker/service-account.yaml
    set:
      imageCredentials:
        secretRef: "frou-frou"
    documentIndex: 0
    asserts:
      - equal:
          path: imagePullSecrets[0].name
          value: "frou-frou"

  # Collector has its own tests for this
  - it: Points all apps to existing slack secret, if provided
    templates:
      - api-server-v2/deployment.yaml
      - operator/deployment.yaml
      - worker/deployment.yaml
    set:
      slackWebhookUrlSecretRefName: "frou-frou"
      slackWebhookUrlSecretRefKey: "url"
    asserts:
      - equal:
          path: $..spec.containers[0].env[?(@.name =~ /SLACK_WEBHOOK_URL$/)].valueFrom.secretKeyRef.name
          value: "frou-frou"
      - equal:
          path: $..spec.containers[0].env[?(@.name =~ /SLACK_WEBHOOK_URL/)].valueFrom.secretKeyRef.key
          value: "url"

  - it: Points all apps to the shared secret when the webhook is pinned in values
    templates:
      - api-server-v2/deployment.yaml
      - operator/deployment.yaml
      - worker/deployment.yaml
    set:
      slackWebhookUrl: "https://hooks.example.com/frou-frou"
    asserts:
      - equal:
          path: $..spec.containers[0].env[?(@.name =~ /SLACK_WEBHOOK_URL$/)].valueFrom.secretKeyRef.name
          value: "thoras-helm-values"
      - equal:
          path: $..spec.containers[0].env[?(@.name =~ /SLACK_WEBHOOK_URL$/)].valueFrom.secretKeyRef.key
          value: "slack-webhook-url"

  - it: Omits the slack env var entirely when no webhook is configured
    templates:
      - api-server-v2/deployment.yaml
      - collector/deployment.yaml
      - operator/deployment.yaml
      - worker/deployment.yaml
    asserts:
      - notContains:
          path: $..spec.containers[-1].env
          content:
            name: SERVICE_SLACK_WEBHOOK_URL
          any: true

  - it: Omits the cloud-sync env var entirely when no cluster key is configured
    templates:
      - api-server-v2/deployment.yaml
      - operator/deployment.yaml
      - worker/deployment.yaml
    asserts:
      - notContains:
          path: $..spec.containers[-1].env
          content:
            name: SERVICE_CLOUD_SYNC_CLUSTER_KEY
          any: true

  - it: Points all apps to the configured cloud-sync secret
    templates:
      - api-server-v2/deployment.yaml
      - operator/deployment.yaml
      - worker/deployment.yaml
    set:
      cloudSync:
        clusterKeySecretRefName: "frou-frou"
        clusterKeySecretRefKey: "clusterKey"
    asserts:
      - equal:
          path: $..spec.containers[0].env[?(@.name == 'SERVICE_CLOUD_SYNC_CLUSTER_KEY')].valueFrom.secretKeyRef.name
          value: "frou-frou"
      - equal:
          path: $..spec.containers[0].env[?(@.name == 'SERVICE_CLOUD_SYNC_CLUSTER_KEY')].valueFrom.secretKeyRef.key
          value: "clusterKey"

  - it: Should set the SERVICE_CLUSTER_NAME environment variables
    templates:
      - api-server-v2/deployment.yaml
      - worker/deployment.yaml
    set:
      cluster:
        name: "test_dev"
    asserts:
      - equal:
          path: $..spec.containers[-1].env[?(@.name =~ /SERVICE_CLUSTER_NAME$/)].value
          value: "test_dev"
